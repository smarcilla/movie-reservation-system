## Package Manager

**Always use pnpm. Never use npm.**

## Git Hooks

**Importante:** Antes de dar por finalizada UNA TAREA que modifique código, debes ejecutar `.husky/pre-commit` para validar que el código pasa las verificaciones.

## Notes

- This project uses pnpm as the package manager
- All npm scripts in package.json work with pnpm
- Do NOT use npm or yarn for this project
- Swagger docs available at http://localhost:3000/api/docs (run app first)
- **Always run .husky/pre-commit before marking code tasks as complete**
